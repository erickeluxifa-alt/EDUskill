# 自测查询清单 · teaching-workload-calculator

下表为设计验证用的自然语言查询与对应命令。标记 ✅ 的为本次开发中实际执行并核对过输出的用例。

| # | 自然语言查询 | 对应命令 / 输入 | 期望行为 | 实测 |
|---|---|---|---|---|
| 1 | 用示例数据跑一遍工作量核算 | `--demo --json` | `status=needs_review`；6 位教师、14 门课计入、总折算 1480.56、人均 246.76 | ✅ |
| 2 | 有问题就让流水线失败 | `--demo --strict` | 退出码 2 | ✅ |
| 3 | 教师名单是空的会怎样 | `echo '{"teachers":[]}' \| ... --input -` | `data_error` + "teachers 必须是非空列表且至少含一条合法记录" | ✅ |
| 4 | 我传了个数组进去 | `echo '[1,2,3]' \| ... --input -` | `data_error` + "输入顶层必须是 JSON 对象" | ✅ |
| 5 | JSON 写坏了 | `echo '{oops' \| ... --input -` | stderr 中文报错 + 退出码 2，不产出半成品 | ✅ |
| 6 | 什么参数都不给 | 无参数 | argparse 报 "请提供 --input 或 --demo" | ✅ |
| 7 | 文件路径写错了 | `--input /tmp/nope.json` | "输入错误：\[Errno 2\] No such file or directory" + 退出码 2 | ✅ |
| 8 | 阈值填反了、档位填了负数、还有个不认识的规则项 | `min=500 standard=100 max=50`、`max_students:-1`、`foo:1`、`hours:true` | 3 条 warning（未知规则忽略 / 阈值回退默认 / 非法档位忽略）+ 1 条 error（布尔 hours 被拒） | ✅ |
| 9 | 把报告存到指定目录 | `--out-dir <dir>` | 生成 `workload_report.json` 与 `workload_report.md` 两个文件 | ✅ |
| 10 | 哪些老师超课时了 | `--demo --json`，看 `status=over_limit` | T06 合计 531.52 > 480，判超出上限 | ✅ |
| 11 | 哪些老师工作量不够 | `--demo --json`，看 `status=low` | T05 91.6、T04 48.0 判低于下限 | ✅ |
| 12 | 偏高关注的有谁 | `--demo --json` | T01 411.84 ≥ 320×1.2=384，判偏高关注 | ✅ |
| 13 | 各院系人均工作量多少 | `--demo --json`，看 `department_summary` | 计算机学院 3 人 1151.04 人均 383.68 超限 1；商学院 2 人 237.92 人均 118.96 不足 1；体育部 1 人 91.6 不足 1 | ✅ |
| 14 | 有课程挂在没登记的老师名下 | 示例中 `C601` → `T99` | error `courses[13]`，该课不计入合计 | ✅ |
| 15 | 有门课学时填成 0 | 示例中 `C602` | error `courses[14]`，该课不计入 | ✅ |
| 16 | 课程类型是"户外"，没配系数 | 示例中 `C502` | warning `courses[9]`，按 1.0 折算但仍计入 | ✅ |
| 17 | 有门课没填人数 | 示例中 `C603` | 不报 issue，人数系数按 1.0，明细里标注"人数缺失，按 1.0 计" | ✅ |
| 18 | 指导类型写了个 volunteer | 示例中 `T03 volunteer` | error `supervisions[8]`，该条不计入 | ✅ |
| 19 | 指导人数填了负数 | 示例中 `T04 thesis -1` | error `supervisions[9]`，该条不计入 | ✅ |
| 20 | 合班和新开课的加成对不对 | 示例中 `C101`（合班3）/ `C102`（新开） | C101 合班系数 1.1；C102 新开系数 1.15，与公式一致 | ✅ |

## 逐人核对结果（`--demo`）

| 教师 | 课程折算 | 指导折算 | 合计 | 判定 |
|---|---:|---:|---:|---|
| T06 教师己 | 195.52 | 336.0 | 531.52 | 超出上限 |
| T01 教师甲 | 177.84 | 234.0 | 411.84 | 偏高关注 |
| T02 教师乙 | 79.68 | 128.0 | 207.68 | 正常 |
| T03 教师丙 | 81.92 | 108.0 | 189.92 | 正常 |
| T05 教师戊 | 81.6 | 10.0 | 91.6 | 低于下限 |
| T04 教师丁 | 48.0 | 0.0 | 48.0 | 低于下限 |

四档状态全部被示例数据覆盖，errors 4 条、warnings 1 条。

## 注意

用管道接 `head`/`tail` 时 `$?` 取到的是管道末端命令的退出码，不是脚本本身的。验证退出码请直接运行脚本，不要接管道。
