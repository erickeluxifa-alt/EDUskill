# 自测记录 SELF_TEST — 实验室预约防冲突排程器 v1.0.0

- 日期: 2026-08-20 · 脚本: `scripts/run_selftest.sh` · 结论: **PASS=29 FAIL=0（100%）**
- 覆盖分支: 常规任务 / 同义触发 / 组合调用 / 缺少输入 / 边界压测 / 异常数据 / 权限与依赖不可用 / 预览确认（默认只读，`--out` 显式写盘已验证）

## 用例明细

| # | 用例 | 类型 | 预期 | 实际 | 通过 |
|---|---|---|---|---|---|
| 1 | resolved_ok | 常规主流程（3 单全解决） | exit 0，`全部解决 3` | 一致 | ✅ |
| 2 | synonyms | 同义触发（中文容器键 实验室/预约申请 + 工位数/座位数 等 5 组别名） | exit 0，`全部解决 2` | 一致 | ✅ |
| 3 | date_formats | 三种日期形态混用 | exit 0，`全部解决 3` | 一致 | ✅ |
| 4 | slot_alias | 时段别名（起始时间/序号/全名） | exit 0，`全部解决 3` | 一致 | ✅ |
| 5 | teacher_conflict_off | 组合调用：教师互斥关闭 + 独占放开，同教师同槽 3 单 | exit 0，张伟×3 | 一致 | ✅ |
| 6 | teacher_conflict_on | 默认教师互斥开启 → 同槽同教师仅排一单 | exit 1，含`缺口` | 一致 | ✅ |
| 7 | empty_input | 缺少输入：空对象 | exit 2，`缺少 resources` | 一致 | ✅ |
| 8 | no_requests | 缺少输入：仅资源 | exit 2，`没有可排的预约申请` | 一致 | ✅ |
| 9 | missing_date | 缺少输入：无日期 | exit 2，`日期缺失` | 一致 | ✅ |
| 10 | missing_students | 缺少输入：无人数 | exit 2，`人数缺失` | 一致 | ✅ |
| 11 | missing_resources | 缺少输入：仅申请 | exit 2，`没有可用实验室资源` | 一致 | ✅ |
| 12 | empty_resources | 边界：资源的数位 0 | exit 2，`为空列表` | 一致 | ✅ |
| 13 | bad_resources | 异常数据：资源含非对象条目 | exit 2，`不是对象` | 一致 | ✅ |
| 14 | exact_capacity | 边界：人数=容量 精确匹配 | exit 0，`全部解决 1` | 一致 | ✅ |
| 15 | split_two_labs | 边界：90 人跨两实验室同槽拆分 | exit 0，`全部解决 1` | 一致 | ✅ |
| 16 | split_disabled | 边界：关闭拆分且容量不足 | exit 1，含`缺口` | 一致 | ✅ |
| 17 | big_class | 边界：100 人大开班跨资源拆分 | exit 0，`全部解决 1` | 一致 | ✅ |
| 18 | huge_class | 边界：2000 人超总量压测 | exit 1，`缺口 1350` | 一致 | ✅ |
| 19 | zero_students | 异常数据：人数=0 | exit 2，`人数缺失或非法` | 一致 | ✅ |
| 20 | negative_students | 异常数据：负人数 | exit 2，`人数缺失或非法` | 一致 | ✅ |
| 21 | closed_date | 边界：资源开放日期不含申请日期 | exit 1，含`缺口` | 一致 | ✅ |
| 22 | missing_equipment | 边界：所需设备无资源满足 | exit 1，含`缺口` | 一致 | ✅ |
| 23 | no_matching_type | 边界：类型无匹配资源 | exit 1，含`缺口` | 一致 | ✅ |
| 24 | bad.json | 异常数据：坏 JSON 文本 | exit 2，`解析失败` | 一致 | ✅ |
| 25 | requests_not_list | 异常数据：requests 非列表 | exit 2，`必须为列表` | 一致 | ✅ |
| 26 | custom_slots | 组合调用：自定义时段表 + 序号引用 | exit 0，含`15:00-17:00` | 一致 | ✅ |
| 27 | priority_first | 组合调用：优先级排序下两单均解决 | exit 0，含`高优先课` | 一致 | ✅ |
| 28 | xss | 安全：`<script>`/`onerror`/`\|` 注入文本 | exit 0，输出含 `&lt;script&gt;`，不含 `<script>` | 一致 | ✅ |
| 29 | sample_main_combo | 组合主样例：中英别名+日期形态+已排定占用+设备/开放日+跨槽拆分+缺口揭示 | exit 1，含`缺口 5` 与数控跨槽条目 | 一致 | ✅ |

## 验证的交互/副作用

- 预览与确认：CLI 默认只读，`--json`/`--out` 显示写盘；已验证 `--out` 落盘文件结构合法（JSON 可解析、输出内容与 stdout 一致）
- 确定性：同一输入两次运行输出逐字节一致（diff 为空）
- 依赖不可用（等价场景）：输入文件不存在 → exit 2 并打印 `[ERROR] 输入解析失败`，不崩溃

## 修复记录（本轮调试发现的 3 个问题）

1. 直接执行 `.py` 缺少可执行权限 → 自测脚本统一经 `python3` 调用（用例最初 28/28 全 FAIL，修复后全 PASS）
2. `max_per_resource_slot=1` 独占规则与"同一资源同槽位多单"测试预期冲突 → 用例调整，并新增规则覆盖说明（可配置 3+ 即放开）
3. 错误提示文案与断言不匹配（"人数缺失或非法" 与 "人数非法"）→ 修正断言为完整文案